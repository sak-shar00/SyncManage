"use strict";Object.defineProperty(exports, "__esModule", { value: true });exports.default = void 0;await jitiImport("dotenv/config");

const DATABASE_URL = process.env.DATABASE_URL;
const DIRECT_URL = process.env.DIRECT_URL;

if (!DATABASE_URL) {
  // Let Prisma CLI fail with a clear message if the env var is not set.
  throw new Error("Missing env var DATABASE_URL");
}var _default = exports.default =

{
  schema: "prisma/schema.prisma",
  datasource: {
    db: {
      url: DATABASE_URL,
      ...(DIRECT_URL ? { directUrl: DIRECT_URL } : {})
    }
  }
}; /* v9-bc65a31bef5bf73e */
